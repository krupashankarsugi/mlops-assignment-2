# Data & models are versioned with DVC, not Git.
/data/raw/
/data/processed/
*.zip
*.mov
*.mp4

# Model artifacts. The served checkpoint is committed so a fresh clone can run
# the service without retraining; other checkpoints stay out of Git.
models/*.pth
models/*.onnx
!models/model.pt

# Experiment tracking
mlruns/
mlartifacts/
.dvc/cache/
.dvc/tmp/
.dvc/config.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# OS / editor
.DS_Store
.idea/
.vscode/

# Backup of Assignment 1's git history, kept locally after re-initialising.
/.git.assignment1.bak/

# Assignment 1 lives here for local reference only; it has its own repository
# at github.com/krupashankarsugi/heart-disease-mlops.
/assignment_1/

# Submission bundle is a build artifact (make package rebuilds it).
MLOps_Assignment02_*.zip
